name: minimal_omi
description: Minimal OMI app for audio streaming
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_blue_plus: 1.33.6
  web_socket_channel: ^3.0.3
  permission_handler: ^12.0.0+1
  provider: ^6.1.2
  collection: ^1.19.1
  flutter_foreground_task: ^8.0.0
  wakelock_plus: ^1.2.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

